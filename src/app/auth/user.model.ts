

export interface User {
    email: string;
    password: string;
    // Add other fields as necessary, e.g., name, username
   }

   // Assuming your login response looks something like this:
// { token: 'your_token_here' }

